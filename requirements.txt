# Dependências do projeto (Python 3.10 ou superior).
#
# Instale com:  pip install -r requirements.txt
mysql-connector-python>=9.0   # driver do MySQL
pydantic>=2.0                 # validação dos dados do livro
