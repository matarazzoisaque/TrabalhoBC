# Dependências do projeto.
#
# Python 3.10 ou superior.
#
# O único pacote externo é o driver oficial de MySQL para Python. Todo o
# restante (servidor HTTP, JSON, testes) usa apenas a biblioteca padrão.
mysql-connector-python>=9.0
